name: Watchtower Daily

on:
  schedule:
    # 早报：UTC 00:30 = 北京时间 08:30
    - cron: '30 0 * * *'
    # 早报兜底：UTC 02:00 = 北京时间 10:00（GitHub 定时任务高峰期可能延迟）
    - cron: '0 2 * * *'
    # 晚报：UTC 12:00 = 北京时间 20:00
    - cron: '0 12 * * *'
    # 晚报兜底：UTC 14:30 = 北京时间 22:30
    - cron: '30 14 * * *'
  workflow_dispatch:
    inputs:
      slot:
        description: '推送时段：morning 早报 / evening 晚报'
        required: false
        default: 'morning'

permissions:
  contents: write

concurrency:
  group: watchtower
  cancel-in-progress: false

jobs:
  watchtower:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run watchtower
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          SCHEDULE_EXPR: ${{ github.event.schedule }}
          DISPATCH_SLOT: ${{ inputs.slot }}
        run: python watchtower/main.py

      # 微信推送已按用户要求停用（2026-08-19）。
      # 需要恢复时取消下面注释，并确认 4 个 WECHAT_* Secrets 已配置：
      # - name: Push to WeChat
      #   env:
      #     WECHAT_APPID: ${{ secrets.WECHAT_APPID }}
      #     WECHAT_APPSECRET: ${{ secrets.WECHAT_APPSECRET }}
      #     WECHAT_OPENID: ${{ secrets.WECHAT_OPENID }}
      #     WECHAT_TEMPLATE_ID: ${{ secrets.WECHAT_TEMPLATE_ID }}
      #   run: python watchtower/wechat_push.py || echo "wechat push failed (non-fatal)"

      - name: Push to Bark (iOS)
        env:
          BARK_DEVICE_KEY: ${{ secrets.BARK_DEVICE_KEY }}
          SCHEDULE_EXPR: ${{ github.event.schedule }}
          DISPATCH_SLOT: ${{ inputs.slot }}
        run: python watchtower/bark_push.py || echo "bark push failed (non-fatal)"

      - name: Commit report
        run: |
          git config user.name "watchtower-bot"
          git config user.email "watchtower-bot@users.noreply.github.com"
          git add data reports
          if git diff --cached --quiet; then
            echo "no changes"
          else
            git commit -m "watchtower: $(TZ=Asia/Shanghai date +%F) daily report"
            git push
          fi
